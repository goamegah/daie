name: CD - Déploiement Databricks

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environnement de déploiement'
        required: true
        default: 'dev'
        type: choice
        options: [dev, test, prod]

      developer_name:
        description: 'Nom du développeur (ex: godwin)'
        required: false
        default: 'dev'
        type: string

      skip_tests:
        description: 'Ignorer les tests (uniquement pour dev)'
        required: false
        default: false
        type: boolean

env:
  PYTHON_VERSION: '3.11'
  CATALOG_PREFIX: 'daie_chn'

permissions:
  contents: read

# ============================================================
# TESTS
# ============================================================
jobs:
  test:
    name: Tests avant déploiement
    runs-on: ubuntu-latest
    if: ${{ !(github.event.inputs.environment == 'dev' && github.event.inputs.skip_tests == 'true') }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'

      - name: Install test dependencies
        run: |
          pip install -U pip
          pip install -r test-requirements.txt
          pip install -e . --no-deps

      - name: Run tests
        run: pytest tests/ -v --cov=src --cov-report=term-missing

# ============================================================
# BUILD
# ============================================================
  build:
    name: Build Python package
    runs-on: ubuntu-latest
    needs: test
    if: always() && (needs.test.result == 'success' || needs.test.result == 'skipped')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Build wheel package
        run: |
          pip install -U pip build wheel
          python -m build
          
          echo ""
          echo "📦 Package built:"
          ls -lh dist/

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-wheel
          path: dist/*.whl
          retention-days: 7
          if-no-files-found: error

# ============================================================
# DEPLOY
# ============================================================
  deploy:
    name: Déploiement sur Databricks
    runs-on: ubuntu-latest
    needs: build
    environment: ${{ github.event.inputs.environment }}

    env:
      ENV_NAME: ${{ github.event.inputs.environment }}
      DEVELOPER_NAME: ${{ github.event.inputs.developer_name }}
      
      # Variables Databricks (depuis GitHub Variables)
      DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
      
      # Authentification Azure AD Service Principal (depuis GitHub Secrets)
      DATABRICKS_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
      DATABRICKS_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
      DATABRICKS_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Download wheel artifact
        uses: actions/download-artifact@v4
        with:
          name: python-wheel
          path: dist/

      # --------------------------------------------------------
      # Install Databricks CLI
      # --------------------------------------------------------
      - name: Install Databricks CLI
        run: |
          pip install -U pip
          pip install databricks-cli databricks-sdk
          
          echo ""
          echo "✅ Databricks CLI installed"
          databricks --version

      # --------------------------------------------------------
      # Configure Databricks authentication via ENV VARS
      # --------------------------------------------------------
      - name: Configure Databricks authentication
        run: |
          # Vérifier les variables d'environnement requises
          if [[ -z "$DATABRICKS_HOST" ]]; then
            echo "❌ DATABRICKS_HOST is not set"
            exit 1
          fi
          
          if [[ -z "$DATABRICKS_CLIENT_ID" ]]; then
            echo "❌ DATABRICKS_CLIENT_ID is not set"
            exit 1
          fi
          
          # Afficher les informations (sanitized)
          echo ""
          echo "✅ Environment variables configured:"
          echo "   DATABRICKS_HOST: $DATABRICKS_HOST"
          echo "   DATABRICKS_CLIENT_ID: ${DATABRICKS_CLIENT_ID:0:8}***"
          echo ""
          echo "ℹ️  Databricks CLI will use these environment variables for authentication"

      # --------------------------------------------------------
      # Test Databricks connection
      # --------------------------------------------------------
      - name: Test Databricks connection
        run: |
          echo "🔍 Testing connection to Databricks workspace..."
          echo ""
          
          # Test avec la commande workspace list
          echo "🧪 Testing workspace connection..."
          if databricks workspace list / 2>&1 | head -5; then
            echo ""
            echo "✅ Connection successful"
          else
            echo ""
            echo "❌ Connection failed"
            exit 1
          fi

      # --------------------------------------------------------
      # Compute deployment variables
      # --------------------------------------------------------
      - name: Compute deployment variables
        id: vars
        run: |
          # Définir les variables de déploiement
          BRONZE_CATALOG="${CATALOG_PREFIX}_${ENV_NAME}_bronze"
          VOLUME_BASE="/Volumes/${BRONZE_CATALOG}/artifacts/packages"
          VOLUME_PATH="${VOLUME_BASE}/${DEVELOPER_NAME}"
          
          # Récupérer le nom du wheel
          WHEEL_FILE=$(ls dist/*.whl | head -1)
          WHEEL_NAME=$(basename "$WHEEL_FILE")
          
          # Exporter les variables
          echo "BRONZE_CATALOG=${BRONZE_CATALOG}" >> $GITHUB_OUTPUT
          echo "VOLUME_PATH=${VOLUME_PATH}" >> $GITHUB_OUTPUT
          echo "WHEEL_FILE=${WHEEL_FILE}" >> $GITHUB_OUTPUT
          echo "WHEEL_NAME=${WHEEL_NAME}" >> $GITHUB_OUTPUT
          
          # Afficher les informations
          echo ""
          echo "📦 Deployment variables:"
          echo "   Catalog: ${BRONZE_CATALOG}"
          echo "   Volume: ${VOLUME_PATH}"
          echo "   Package: ${WHEEL_NAME}"

      # --------------------------------------------------------
      # Create Volume directory structure
      # --------------------------------------------------------
      - name: Create Volume directory
        run: |
          VOLUME_PATH="${{ steps.vars.outputs.VOLUME_PATH }}"
          
          echo "📁 Creating volume directory: ${VOLUME_PATH}"
          
          # Créer le répertoire dans le volume Unity Catalog
          databricks fs mkdirs "dbfs:${VOLUME_PATH}" || {
            echo "⚠️  Directory might already exist, continuing..."
          }

      # --------------------------------------------------------
      # Upload wheel to Unity Catalog Volume
      # --------------------------------------------------------
      - name: Upload wheel to Unity Catalog Volume
        run: |
          set -e
          
          WHEEL_FILE="${{ steps.vars.outputs.WHEEL_FILE }}"
          VOLUME_PATH="${{ steps.vars.outputs.VOLUME_PATH }}"
          WHEEL_NAME="${{ steps.vars.outputs.WHEEL_NAME }}"
          
          echo ""
          echo "📤 Uploading wheel package..."
          echo "   Source: ${WHEEL_FILE}"
          echo "   Destination: dbfs:${VOLUME_PATH}/${WHEEL_NAME}"
          echo ""
          
          # Upload du fichier wheel avec overwrite
          databricks fs cp "${WHEEL_FILE}" "dbfs:${VOLUME_PATH}/${WHEEL_NAME}" --overwrite
          
          echo ""
          echo "✅ Upload successful"

      # --------------------------------------------------------
      # Verify upload
      # --------------------------------------------------------
      - name: Verify upload
        run: |
          VOLUME_PATH="${{ steps.vars.outputs.VOLUME_PATH }}"
          WHEEL_NAME="${{ steps.vars.outputs.WHEEL_NAME }}"
          
          echo ""
          echo "🔍 Verifying upload..."
          echo ""
          
          # Lister le contenu du volume
          if databricks fs ls "dbfs:${VOLUME_PATH}/"; then
            echo ""
            echo "✅ Package verified in volume"
          else
            echo ""
            echo "❌ Failed to verify package"
            exit 1
          fi

      # --------------------------------------------------------
      # Deployment summary
      # --------------------------------------------------------
      - name: Deployment summary
        if: always()
        run: |
          WHEEL_NAME="${{ steps.vars.outputs.WHEEL_NAME }}"
          BRONZE_CATALOG="${{ steps.vars.outputs.BRONZE_CATALOG }}"
          VOLUME_PATH="${{ steps.vars.outputs.VOLUME_PATH }}"
          
          echo ""
          echo "╔════════════════════════════════════════════════════════╗"
          echo "║        ✅ DÉPLOIEMENT DATABRICKS TERMINÉ               ║"
          echo "╚════════════════════════════════════════════════════════╝"
          echo ""
          echo "📋 Informations de déploiement:"
          echo "   • Environment    : ${{ env.ENV_NAME }}"
          echo "   • Developer      : ${{ env.DEVELOPER_NAME }}"
          echo "   • Catalog        : ${BRONZE_CATALOG}"
          echo "   • Volume         : ${VOLUME_PATH}"
          echo "   • Package        : ${WHEEL_NAME}"
          echo ""
          echo "🔗 Databricks:"
          echo "   • Workspace      : ${{ env.DATABRICKS_HOST }}"
          echo ""
          echo "📝 Git:"
          echo "   • Commit         : ${{ github.sha }}"
          echo "   • Branch         : ${{ github.ref_name }}"
          echo "   • Actor          : ${{ github.actor }}"
          echo ""
          echo "╔════════════════════════════════════════════════════════╗"
          echo "║  📦 INSTALLATION DANS UN NOTEBOOK DATABRICKS          ║"
          echo "╚════════════════════════════════════════════════════════╝"
          echo ""
          echo "%pip install /Volumes/${BRONZE_CATALOG}/artifacts/packages/${{ env.DEVELOPER_NAME }}/${WHEEL_NAME}"
          echo ""
          echo "╚════════════════════════════════════════════════════════╝"
          echo ""

      # --------------------------------------------------------
      # Notification (optionnel)
      # --------------------------------------------------------
      - name: Send notification
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "Check the logs above for more details."